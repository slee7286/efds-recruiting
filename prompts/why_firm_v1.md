# Why-firm argument task

Create evidence-backed argument building blocks, not unsupported application prose. A specificity score of 4 requires company source IDs; a score of 5 requires both company source IDs and candidate evidence IDs. Return JSON matching the declared schema and flag weak evidence.
