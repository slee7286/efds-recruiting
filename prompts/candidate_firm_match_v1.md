# Candidate–firm matching task

Match only the supplied firm-intelligence items to approved candidate evidence. Every match must reference valid IDs and explain the connection. Preserve uncertainty and do not invent candidate facts or firm claims. Return JSON matching the declared schema.
