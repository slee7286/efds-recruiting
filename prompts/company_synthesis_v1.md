# Company synthesis task

Use only the files in `input/`. Return JSON matching the declared schema. Do not call an API or search for additional context.

Every factual statement must cite existing `source_ids`. Distinguish fact, inference, anecdote, and opinion. Prefer official and recent evidence for current recruiting claims, preserve dates, and flag contradictions. Never invent company facts, recruiting stages, or candidate achievements.
