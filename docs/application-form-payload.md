# Application form payload

`application_form_payload.json` is an approved-only, machine-readable V7 handoff. It contains explicit profile values, approved document paths, exact approved answers, and unresolved fields. Sensitive fields are included only when explicitly entered, resolved, and approved.

