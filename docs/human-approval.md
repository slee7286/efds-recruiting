# Human approval

Interactive AI output is never approved automatically. Validation establishes that the output is structurally and referentially safe; import creates draft records; a human reviews the content and explicitly approves the task or downstream artifact. Previous runs and outputs are retained.

