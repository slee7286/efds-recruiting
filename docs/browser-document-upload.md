# Browser document upload

Only approved packet artifacts are eligible for upload. The packet verifier
checks ownership and hashes first; the browser records artifact ID, expected
hash, filename, and verification status locally. Filename recency heuristics
are not used. Unknown transcript, portfolio, or supporting-document requests
pause for human selection.
