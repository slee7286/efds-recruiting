# Application packets

`recruiting artifact build-packet <application-id>` assembles approved artifacts, deterministic summaries, answers, and the form payload under `applications/<id>/packet/`. The manifest records artifact IDs, hashes, paths, unresolved fields, and packet version. It never submits an application.

