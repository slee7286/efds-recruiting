# EFDS shared API integration

The future EFDS website should consume the same public `/api/v1` contract for
companies, jobs, recruiting timelines, resources, and public interview
preparation. It must not receive local candidate data or direct database
credentials. Private profile, applications, CVs, answers, AI history, browser
runs, and outcomes remain in the local companion.
