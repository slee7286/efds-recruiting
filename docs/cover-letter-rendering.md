# Cover-letter rendering

Cover letters are structured approved blocks (`opening`, `why_company`, `why_role`, `candidate_fit`, and `closing`) with candidate/source joins. V6 renders Markdown, LaTeX, and PDF only from approved blocks; it does not generate or paraphrase text.

