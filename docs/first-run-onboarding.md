# First-run onboarding

The local companion's onboarding form stores optional preferred name, email, and
university fields only in local SQLite. Shared intelligence configuration and
AI provider preference are local settings. Profile fields are not required and
are never inferred from public research. A future packaged launcher can wrap
the same initialization sequence without changing the data boundary.
