# Local job alerts

Job-alert rules and matches are local records. Filters can include companies,
role families, locations, cycles, and keywords. The background runner matches
rules against the locally cached public jobs and creates local notifications.
User preferences are not sent upstream. A new match does not create an
application or trigger research automatically.
