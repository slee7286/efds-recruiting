# Browser field mapping

Mappings preserve the employer's original label and record normalized key,
source key, method, confidence, reason, expected hash, and result. Exact and
high-confidence aliases may fill. Medium, low, and unmapped fields pause or
skip only when optional. User aliases and application-only answers are local.

Written questions require an exact original-question match; semantic guessing
is not used.
