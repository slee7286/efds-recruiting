# Offline mode

After a pull, local applications, AI bundles, artifacts, review, and browser
handoff continue without PostgreSQL. Cached public intelligence is usable with
fresh/stale status indicators. Discovery and shared refresh fail clearly when
the shared service is unavailable; they do not block private work.
