# SmartRecruiters autofill boundary

SmartRecruiters is recognized from its public host and application markers.
V12 reuses the accessible generic inspector for stable identity, document, and
simple question fields. Tenant-specific dynamic controls, account gates, and
ambiguous options pause in assisted mode. The adapter never guesses sensitive
or legal answers and never clicks final submission.
