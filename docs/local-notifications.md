# Local notifications

The companion exposes local notifications for deadlines, assessments, interviews,
email, browser, sync, preparation, and system events. Notification text should avoid
private detail by default. Nothing is sent to shared telemetry or the shared API.
