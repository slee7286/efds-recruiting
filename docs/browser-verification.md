# Browser verification

Before readiness, V10 verifies safe fields, exact text values, document file
presence, required controls, visible validation errors, and packet freshness.
Each run writes `form-snapshot.json`, `fill-log.json`, `validation.json`,
`review.html`, and `submission-checklist.md` under the private application
directory.
