# Browser written answers

Application questions are matched by provider identifiers when available or by
exact normalized original wording. The value chain is:

```text
approved local answer = answers.json = payload = browser value
```

Length limits and browser-side truncation are verification failures. Answers
are never shortened or paraphrased during autofill.
