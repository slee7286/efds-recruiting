# Synthetic opportunity-review fixture

This fixture is intentionally fictional and is used only by
`tests/test_opportunity_review.py`. It contains two stable opportunity IDs so
the tests can exercise positive, unknown, conditional, malformed, foreign and
changed-evidence annotation paths without touching the real consolidated pack.
